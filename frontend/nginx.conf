server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback -- client-side routes (e.g. /queues/abc) must resolve to index.html.
    location / {
        try_files $uri /index.html;
    }
}
