DATABASE_URL=postgresql+asyncpg://scheduler:scheduler@localhost:5432/scheduler
JWT_SECRET=change-me-in-production

# Optional -- enables the AI failure summary bonus feature (POST .../ai-summary).
# Get a free key at https://console.groq.com. Without it, that endpoint returns a clean 503.
GROQ_API_KEY=
